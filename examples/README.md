# Examples

This folder contains example apps for OpenTUI. The React examples are in `examples/react`.

How to run a React example (run from the repo root):

```bash
bun install
bun examples/react/basic.tsx
```

If you want to add or update examples for other packages, put them under `examples/<package>`.
